<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.EphramaApplication" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--Composant titre par defaut-->
    <style name="DefaultTextStyle">
        <item name="android:textColor">@color/darkGray</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">@font/kanit</item>
    </style>


    <!--Composant sous-titre par defaut-->
    <style name="SubtitleTextStyle" parent= "DefaultTextStyle">
        <item name="android:textColor">@color/noir</item>
    </style>
    <!--Composant titre par defaut-->
    <style name="TitleTextStyle" parent = "SubtitleTextStyle">
        <item name="android:textSize">24sp</item>
    </style>

    <style name="LoadingPageTextStyle">
        <item name="android:textColor">@color/blanc</item>
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">@font/kanit</item>
    </style>

    <style name="ButtonTextStyle">
        <item name="android:textColor">@color/blanc</item>
        <item name="android:textSize">18dp</item>
        <item name="android:fontFamily">@font/kanit</item>
    </style>
</resources>